import test from 'node:test';import assert from 'node:assert/strict';import {readFileSync} from 'node:fs';import {participantPatch} from '../src/domain.js';
test('participant profile metadata is validated and exposed',()=>{assert.deepEqual(participantPatch({preferredName:' Maria ',profilePhotoUrl:'https://example.org/maria.jpg'}),{preferred_name:'Maria',profile_photo_url:'https://example.org/maria.jpg'});assert.throws(()=>participantPatch({profilePhotoUrl:'http://unsafe.example/photo.jpg'}));const api=readFileSync(new URL('../src/index.js',import.meta.url),'utf8');for(const token of ['preferred_name','profile_photo_url','participantSince','profilePhotoUrl'])assert.ok(api.includes(token));});
test('migration constrains profile metadata',()=>{const sql=readFileSync(new URL('../../database/migrations/048_up.sql',import.meta.url),'utf8');for(const token of ['preferred_name','profile_photo_url',"~ '^https://'",'char_length'])assert.ok(sql.includes(token));});
